package operatos;

public class AreaOfACircle {
    public static void main(String[] args) {
        double r = 2.3d;
        double area = (3.14 * (r*r));
        System.out.println("Area of a circle whose " +
                "radius r = "+ r +" is "+ area);
    }
}
